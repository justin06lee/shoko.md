# All dependencies are optional. Scripts degrade gracefully when a package is
# missing: dedup falls back to pairwise Jaccard without datasketch, length_stats
# falls back to a chars/4 heuristic without tiktoken, and parquet/arrow inputs
# are simply refused without pyarrow.
datasketch>=1.6.5,<2
tiktoken>=0.7.0,<1
pyarrow>=15.0.0,<23
